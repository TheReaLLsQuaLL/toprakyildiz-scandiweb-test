import React from "react";
import "./styles.scss";

const Homepage = (props) => {
  return (
    <section className="homepage">
      <h1>Homepage</h1>
    </section>
  );
};

export default Homepage;
